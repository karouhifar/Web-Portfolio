{{!-- Loading each pages for refreshing HTML DOM --}}
<div class="loader-container  text-light text-uppercase" onload="removeLoading()">
    <div class="spinner-wrapper">
        <div class="spinner">
            <strong class="loading-text px-md-4 display-5">Loading...</strong>
        </div>

    </div>
</div>
{{!-- ------------------------------------------- --}}
<header class="headerMain">
    <div class="main-menu">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand ms-5" href="/">
                    <img id="imgLogo" src="img/logo.svg" alt="logo"/>
                </a>
                <button id="navbarID" class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        {{#if homeData}}
                        <li class="nav-item" style="cursor: pointer;">
                            <span class="nav-link fw-bold" aria-current="page" onclick="scrollToTarget('projects')"><b>Projects</b></span>
                        </li>
                        {{else}}
                        <li class="nav-item" style="cursor: pointer;">
                            <span class="nav-link" aria-current="page" onclick="scrollToTarget('projects')"><b>Projects</b></span>
                        </li>
                        {{/if}}
                        {{#if aboutData}}
                        <li class="nav-item active" style="cursor: pointer;">
                            <span class="nav-link" onclick="scrollToTarget('myStory')"><b>My Story</b></span>
                        </li>
                        {{else}}
                        <li class="nav-item " style="cursor: pointer;">
                            <span class="nav-link"  onclick="scrollToTarget('myStory')"><b>My Story</b></span>
                        </li>
                        {{/if}}
                        {{#if contactData}}
                        <li class="nav-item active" style="cursor: pointer;">
                            <span class="nav-link"onclick="scrollToTarget('contact')"><b>Contact</b></span>
                        </li>
                        {{else}}
                        <li class="nav-item " style="cursor: pointer;">
                            <span class="nav-link"onclick="scrollToTarget('contact')"><b>Contact</b></span>
                        </li>
                        {{/if}}
                        {{#if contactData}}
                        <li class="nav-item active" style="cursor: pointer;">
                            <span class="nav-link" href="/contact"><b>Download CV</b></span>
                        </li>
                        {{else}}
                        <li class="nav-item" style="cursor: pointer;">
                            <span class="nav-link" href="/contact"><b>Download CV</b></span>
                        </li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </nav>
    </div>
</header>